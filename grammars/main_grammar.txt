# General

program -> declarationList
declarationList -> declarationList declaration | declaration
declaration -> varDec | functionDeclaration

# Variable Declarations

varDec -> typeSpecifier ID = expression ;
assignment -> ID = expression ;

# Function Declarations

functionDeclaration -> typeSpecifier ID ( ) { statementList }

# Statements

statementList -> statementList statement | statement
statement -> varDec | returnStatement | ifStatement | assignment
returnStatement -> return expression ;

# Control Flow

ifStatement -> if ( condition ) { statementList } | if ( condition ) { statementList } else { statementList }
condition -> e == e

# Expressions

expression -> t + expression | t - expression | t
t -> e * t | e / t | e
e -> constNum | ID

